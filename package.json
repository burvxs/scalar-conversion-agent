{
  "name": "scalar-mcp",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently \"tsc --watch\" \"nodemon --watch dist --ext js --exec 'node --env-file=.env dist/client.js'\"",
    "start": "node dist/index.js",
    "client": "node dist/client.js",
    "webhook": "node --env-file=.env dist/webhook.js",
    "dev:webhook": "nodemon --watch dist --ext js --exec 'node --env-file=.env dist/webhook.js'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.2.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14",
    "typescript": "^5.0.0"
  }
}
